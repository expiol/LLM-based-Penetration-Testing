# 手动运行调试指南

## 问题现象
- 测试脚本能正常调用 LLM
- 手动运行时卡住，没有进入 LLM 调用

## 可能的原因

### 1. 输出缓冲问题
手动运行时，Python 的输出可能被缓冲，导致看不到实时进度。

**解决方案：**
- 所有 `print()` 都添加了 `flush=True`
- 在关键位置添加了 `sys.stdout.flush()`

### 2. 事件循环线程问题
`AsyncRunner` 在后台线程运行事件循环，可能存在线程同步问题。

**已添加：**
- 事件循环启动确认机制（`_ready_event`）
- 超时保护（10分钟）
- 进度提示

### 3. LLM 调用阻塞
LLM API 调用可能很慢或卡住。

**已添加：**
- LLM 调用超时（5分钟）
- 实时输出刷新
- 详细的错误提示

## 调试步骤

### 步骤1: 检查事件循环
运行测试脚本验证 AsyncRunner：
```bash
conda activate pentest
python test_async_runner.py
```

### 步骤2: 手动运行并观察输出
```bash
conda activate pentest
python scripts/pentest_cli.py
```

观察输出，看卡在哪一步：
- 如果卡在"正在启动异步运行器"，说明事件循环启动有问题
- 如果卡在"初始化框架"，说明框架初始化有问题
- 如果卡在"正在调用主控 LLM"，说明 LLM 调用有问题

### 步骤3: 检查网络和 LLM 服务
确保：
- 网络连接正常
- LLM API 地址可访问
- API Key 有效

## 已做的优化

1. ✅ 移除了 Spinner，让输出实时显示
2. ✅ 添加了进度提示（"等待中..."）
3. ✅ 增加了超时保护（避免无限等待）
4. ✅ 确保所有输出立即刷新
5. ✅ 添加了详细的错误信息

## 如果仍然卡住

1. 检查终端输出，看最后一行是什么
2. 使用 `Ctrl+C` 中断，查看堆栈跟踪
3. 检查日志文件（如果有）
4. 尝试运行 `test_async_runner.py` 验证基础功能

